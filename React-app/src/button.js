import React from 'react'
import { Button } from 'semantic-ui-react'

const ButtonExamplePositive = (props) => (
  <div>
    <Button positive onClick={props.toggle}>Show</Button>
  </div>

  
)

export default ButtonExamplePositive
